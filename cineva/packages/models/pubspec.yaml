name: cineva_models
description: Core domain models for Cineva.
publish_to: none
version: 0.1.0

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  cineva_shared:
    path: ../shared
  equatable: ^2.0.5
  intl: ^0.19.0
  meta: ^1.12.0

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.8
