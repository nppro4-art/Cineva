name: cineva_shared
description: Shared core types and utilities for Cineva.
publish_to: none
version: 0.1.0

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  equatable: ^2.0.5
  meta: ^1.12.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.8
