name: cineva_android_tv
description: Cineva user application for Android TV.
publish_to: none
version: 0.1.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cineva_shared:
    path: ../../packages/shared
  cineva_widgets:
    path: ../../packages/widgets

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
